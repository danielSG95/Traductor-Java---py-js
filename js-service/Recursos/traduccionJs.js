class test {
  var numero;
  var prueba = 0;
  test() {
    numero = 1 + 1;

  }

  main(args) {
    numero = 5 + 6 * (8 / 4) + (a * b / 36) - 50;
    suma = suma(1, 2 * 5);
    imprimir()
    return suma(1, 2 * (5 - 6)) + suma(1, 4);

  }

  imprimir() {
    for (i = 0; i < 10 + 1; i++) {
      console.log("esto es una cadena");
      for (j = 0; j <= 2 * i + 1; j = j + 1;) {
        do {
          if (i >= 5 * j) {
            return;

          }

        }

      }

    }

  }

  suma(a, b) {
    if (a == b && b >= c) {
      return "cadena";

    } else if () {
      return a + b;

    } else if (a) {
      resta = resta(5, 2) + 3;
      console.log(resta);
      if (resta <= 200) {
        console.log("no se que putas estoy haciendo con mi vida");

      }

    } else {
      imprimir()

    }

  }

  isZero(test) {
    return test >= 1;

  }

  main(args) {
    console.log(suma(1, 1));

  }


}
/*JavaScript no soporta interfaces. Imposible traducir :( */